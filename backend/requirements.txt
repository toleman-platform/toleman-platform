# (#239) fastapi pinned past 0.133.0, the first release that drops its own
# upper bound on starlette -- earlier releases capped it below 0.39.0/0.41.0/
# etc, which kept resolving to starlette 0.38.6 no matter how new a fastapi
# was requested. That version carried 7 real CVEs including an SSRF
# (CVE-2026-48818, GHSA-wqp7-x3pw-xc5r), invisible to Trivy because it reads
# requirements.txt's direct pins and starlette was never one -- it arrived
# transitively through fastapi. 0.134.0 verified: full test suite green,
# `pip check` clean, and `@app.on_event` (deprecated as of this bump, see
# app/main.py) migrated to `lifespan=` rather than left on a warning.
fastapi==0.134.0
# Pinned explicitly, not left to resolve transitively, for two reasons: this
# exact package is what #239 was about, so it belongs where Trivy's
# manifest-only scan (see the mechanism note in .github/workflows/self-scan.yml) can see
# it directly rather than only through the fully-resolved-dependency job; and
# a floor without a pin invites the opposite failure someday -- a new fastapi
# release loosening its own bound further while requirements.txt looks
# unchanged. Verified against OSV: 1.3.1 is the lowest version clearing every
# advisory affecting 0.38.6; 1.6.0 is simply what 0.134.0 resolved to and has
# no additional known issues.
starlette==1.6.0
pydantic==2.9.2
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
alembic==1.13.3
psycopg[binary]==3.2.10
celery==5.4.0
redis==5.0.8
pydantic-settings==2.5.2
python-dotenv==1.2.3
httpx==0.27.2
GitPython==3.1.59
anthropic==0.121.0
python-multipart==0.0.32
PyJWT[crypto]==2.13.0
# Test-only: validates the AIBOM (#190) against the published CycloneDX 1.6
# schema. Lives here rather than a separate dev file because this project
# already keeps pytest and pytest-mock in the same list.
jsonschema==4.26.0
pytest==9.1.1
pytest-mock==3.14.0
reportlab==4.2.5
pypdf==6.15.0
PyYAML==6.0.3
modelscan==0.8.8
