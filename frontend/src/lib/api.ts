/**
 * Root API module entrypoint.
 *
 * Deconstructed into domain modules under `@/lib/api/*` and typed via `@/types/*`.
 * Re-exports the backward-compatible `api` facade, individual functions, and all domain types.
 */

export * from "./api/index";
